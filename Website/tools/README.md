# Website Tools

Support files for the Website app.

- `verification/` — Node verification scripts used by `npm run verify:*` and `npm run check`.
- `fixtures/` — static fixtures used by verification scripts, including `button-map.json`.
